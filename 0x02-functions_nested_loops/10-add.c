#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: parameter to be added to b
 * @b: parameter to be added to a
 * Return: results of two added integers
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
