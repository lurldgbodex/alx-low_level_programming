#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @r: integer to check
 * return: the absolute value of integer
 */
int  _abs(int r)
{
	int r;

	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
}
