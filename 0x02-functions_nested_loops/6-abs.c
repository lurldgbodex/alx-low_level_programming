#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @r: integer to check
 * Return: the absolute value of integer
 */
int  _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	return (-r);
}
