#include <stdio.h>

/*
 * main - uses puts to print to standard output
 * Return: 0 if exited properly, non-zero otherwise
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
