#include <stdio.h>

/**
 * main - prints strings with proper grammer,
 * but the outcome is a piece of art,
 * Return: 0 if exited proper, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
